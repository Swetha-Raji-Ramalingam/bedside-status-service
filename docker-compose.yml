services:
  bedside-status:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ConnectionStrings__ContentDb=${ConnectionStrings__ContentDb}
      - TrustId=${TrustId}
    restart: "no"
