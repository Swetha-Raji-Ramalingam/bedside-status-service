# deploy/
Task definition and deployment config go here. The pipeline itself belongs
at the repository root (bitbucket-pipelines.yml). No Terraform required —
note the infrastructure your pipeline assumes in comments.
